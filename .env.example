# PostgreSQL connection string.
# Local dev without this variable falls back to an in-memory database
# (PGlite), whose data is not persisted between restarts.
# DATABASE_URL=postgres://user:password@host:5432/finance
